<%= render "shared/navbar" %>

<div class="tile is-ancestor is-flex is-align-items-center is-justify-content-center">
  <div class="tile is-5 is-vertical is-parent">
    <div class="tile is-child box">
      <p class="title">Tuesday:</p>
      <%= render "shared/dotw", :day => 'Tuesday' %>
    </div>

    <div class="tile is-child box">
      <p class="title">Wednesday:</p>
      <%= render "shared/dotw", :day => 'Wednesday' %>
    </div>

    <div class="tile is-child box">
      <p class="title">Thursday:</p>
      <%= render "shared/dotw", :day => 'Thursday' %>
    </div>
  </div>

  <div class="tile is-5 is-vertical is-parent">
    <div class="tile is-child box">
      <p class="title">Friday:</p>
      <%= render "shared/dotw", :day => 'Friday' %>
    </div>

    <div class="tile is-child box">
      <p class="title">Saturday:</p>
      <%= render "shared/dotw", :day => 'Saturday' %>
    </div>

    <div class="tile is-child box">
      <p class="title">Irregular:</p>
      <%= render "shared/dotw", :day => 'Irregular' %>
    </div>
  </div>
</div>
